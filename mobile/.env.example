# Backend-API (uvicorn backend.api.app:app --reload --host 0.0.0.0).
# WICHTIG: hier die LAN-IP dieses Rechners eintragen, NICHT localhost --
# "localhost" im Handy-Browser meint das Handy selbst, nicht den PC.
# IP herausfinden: ipconfig (Windows) / ifconfig (Mac/Linux).
VITE_API_BASE_URL=http://192.168.2.196:8000

# Kein Auth-System vorhanden (siehe BACKEND_ARCHITECTURE.md §2.7, noch nicht
# gebaut) -- Single-User-Companion-App zeigt fest diesen User an.
VITE_USER_ID=28182fce-d78e-4585-b6be-a33914f7271c
VITE_USER_DISPLAY_NAME=David
